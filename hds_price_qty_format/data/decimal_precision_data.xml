<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <!-- Ajustar precisión de cantidades a 2 decimales -->
        <record id="product.decimal_precision_product_uom" model="decimal.precision">
            <field name="digits">2</field>
        </record>

        <!-- Ajustar precisión de precios unitarios a 2 decimales -->
        <record id="product.decimal_precision_price" model="decimal.precision">
            <field name="digits">2</field>
        </record>

        <!-- Ajustar precisión de descuentos -->
        <record id="product.decimal_precision_discount" model="decimal.precision">
            <field name="digits">2</field>
        </record>
    </data>
</odoo>

