<?xml version="1.0" ?>
<odoo>

    <!-- Mail template are declared in a NOUPDATE block
         so users can freely customize/delete them -->
    <data noupdate="0">
         <record id="mail_template_sale_confirmation_custom" model="mail.template">
            <field name="name">Ventas: Email de confirmación</field>
            <field name="model_id" ref="sale.model_sale_order"/>
            <field name="subject">${object.company_id.name} ${(object.get_portal_last_transaction().state == 'pending') and 'Pedido pendiente' or 'Pedido'} (Ref ${object.name or 'n/a' })</field>
            <field name="email_from">${(object.user_id.email_formatted or user.email_formatted) | safe}</field>
            <field name="partner_to">${object.partner_id.id}</field>
            <field name="body_html" type="html">

                <div style="margin: 0px; padding: 0px;">
                    <p style="margin: 0px; padding: 0px; font-size: 13px;">
                        % set doc_name = 'quotation' if object.state in ('draft', 'sent') else 'order'
                        Hello,
                        <br/>
                        <br/>
                        Your
                        % if ctx.get('proforma'):
                        Pro forma invoice for ${doc_name}
                        <strong>${object.name}</strong>
                        % if object.origin:
                        (with reference: ${object.origin} )
                        % endif
                        amounting in <strong>${format_amount(object.amount_total, object.pricelist_id.currency_id)}
                    </strong> is available.
                        % else:
                        ${doc_name}
                        <strong>${object.name}</strong>
                        % if object.origin:
                        (with reference: ${object.origin} )
                        % endif
                        amounting in <strong>${format_amount(object.amount_total, object.pricelist_id.currency_id)}
                    </strong> has been confirmed.
                        % endif
                        <br/>
                        <br/>
                        Do not hesitate to contact us if you have any questions.
                        <br/>
                    </p>
                    <span>
                        <b>IMPORTANTE !!</b>
                    </span>
                    <br></br>
                    <span>ESTE ES UN EMAIL AUTOMÁTICO Y NO ADMITE RESPUESTA</span>
                    <br></br>
                    <span>POR FAVOR, DIRIGIR CUALQUIER COMUNICACIÓN RELACIONADA A: pdesus@hdsmetalicas.es</span>
                </div>

            </field>
            <field name="report_template" ref="sale.action_report_saleorder"/>
            <field name="report_name">${(object.name or '').replace('/','_')}</field>
            <field name="lang">${object.partner_id.lang}</field>
            <field name="user_signature" eval="False"/>
            <field name="auto_delete" eval="True"/>
        </record>
        <!--Email template -->
        <record id="email_template_edi_sale_custom" model="mail.template">
            <field name="name">Ventas: Enviar por email</field>
            <field name="model_id" ref="sale.model_sale_order"/>
            <field name="subject">${object.company_id.name}
                ${object.state in ('draft', 'sent') and 'Presupuesto' or 'Pedido'} (Ref ${object.name or 'n/a' })
            </field>
            <field name="email_from">${(object.user_id.email_formatted or user.email_formatted) | safe}</field>
            <field name="partner_to">${object.partner_id.id}</field>
            <field name="body_html" type="html">
                <div style="margin: 0px; padding: 0px;">
                    <p style="margin: 0px; padding: 0px; font-size: 13px;">
                        % set doc_name = 'quotation' if object.state in ('draft', 'sent') else 'order'
                        Hello,
                        <br/>
                        <br/>
                        Your
                        % if ctx.get('proforma'):
                        Pro forma invoice for ${doc_name}
                        <strong>${object.name}</strong>
                        % if object.origin:
                        (with reference: ${object.origin} )
                        % endif
                        amounting in <strong>${format_amount(object.amount_total, object.pricelist_id.currency_id)}
                    </strong> is available.
                        % else:
                        ${doc_name}
                        <strong>${object.name}</strong>
                        % if object.origin:
                        (with reference: ${object.origin} )
                        % endif
                        amounting in <strong>${format_amount(object.amount_total, object.pricelist_id.currency_id)}
                    </strong> is ready for review.
                        % endif
                        <br/>
                        <br/>
                        Do not hesitate to contact us if you have any questions.
                        <br/>
                    </p>
                    <span>
                        <b>IMPORTANTE !!</b>
                    </span>
                    <br></br>
                    <span>ESTE ES UN EMAIL AUTOMÁTICO Y NO ADMITE RESPUESTA</span>
                    <br></br>
                    <span>POR FAVOR, DIRIGIR CUALQUIER COMUNICACIÓN RELACIONADA A: pdesus@hdsmetalicas.es</span>
                </div>
            </field>
            <field name="report_template" ref="sale.action_report_saleorder"/>
            <field name="report_name">${(object.name or '').replace('/','_')}</field>
            <field name="lang">${object.partner_id.lang}</field>
            <field name="user_signature" eval="False"/>
            <field name="auto_delete" eval="True"/>
        </record>
    </data>
</odoo>
